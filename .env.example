# Leave empty for local development: Vite proxies API calls to Express on port 3001.
# Set this only when building the frontend against a separately hosted API.
# VITE_API_URL=https://example.execute-api.eu-west-2.amazonaws.com
