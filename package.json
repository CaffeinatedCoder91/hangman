{
  "name": "hangman",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w api\" \"npm run dev -w web\"",
    "lint": "oxlint packages --max-warnings=0",
    "check": "npm run lint && npm run test -w api && npm run type-check -w api && npm run type-check -w web && npm run build -w web",
    "deploy:staging": "echo 'deploy:staging lands in session 5 (sst deploy --stage staging)' && exit 1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "oxlint": "^1.71.0"
  }
}
